"use strict"
const ratings = document.querySelectorAll('.rating')
console.log(ratings.length + " <--это количество рейтингов на данный момент")
// Проверяем на наличие рэйтингов и запускаем функцию
if (ratings.length > 0) {
  let idtest = element.id('.rating_body')
  console.log(idtest);
}
